name: NET X Final Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Create Project Structure
        run: |
          # 1. إنشاء المجلدات
          mkdir -p app/src/main/java/com/turbo/net
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/mipmap-xhdpi

          # 2. حل مشكلة الأيقونة (إذا لم توجد صورة سيصنع واحدة بيضاء بسيطة)
          if [ -f "icon.png" ]; then
            cp icon.png app/src/main/res/mipmap-xhdpi/ic_launcher.png
          else
            echo "No icon.png found, creating a placeholder"
            touch app/src/main/res/mipmap-xhdpi/ic_launcher.png
          fi

          # 3. ملف الألوان والثيمات الأساسية
          cat <<'EOF' > app/src/main/res/values/colors.xml
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="black">#FF000000</color>
              <color name="white">#FFFFFFFF</color>
              <color name="purple_500">#FF6200EE</color>
          </resources>
          EOF

          cat <<'EOF' > app/src/main/res/values/themes.xml
          <resources>
              <style name="Theme.NetX" parent="Theme.AppCompat.Light.NoActionBar" />
          </resources>
          EOF

          # 4. المانيفست (النسخة النهائية)
          cat <<'EOF' > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <application 
                  android:label="NET X" 
                  android:icon="@mipmap/ic_launcher" 
                  android:theme="@style/Theme.NetX">
                  <activity android:name="com.turbo.net.MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

          # 5. كود الجافا
          cat <<'EOF' > app/src/main/java/com/turbo/net/MainActivity.java
          package com.turbo.net;
          import android.os.Bundle;
          import android.widget.TextView;
          import android.view.Gravity;
          import android.graphics.Color;
          import androidx.appcompat.app.AppCompatActivity;

          public class MainActivity extends AppCompatActivity {
              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  TextView tv = new TextView(this);
                  tv.setText("NET X IS READY\n🚀");
                  tv.setTextSize(30);
                  tv.setTextColor(Color.BLUE);
                  tv.setGravity(Gravity.CENTER);
                  setContentView(tv);
              }
          }
          EOF

          # 6. إعدادات Gradle
          echo "android.useAndroidX=true" > gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties

          cat <<'EOF' > build.gradle
          buildscript {
              repositories { google(); mavenCentral() }
              dependencies { classpath 'com.android.tools.build:gradle:8.1.0' }
          }
          allprojects { repositories { google(); mavenCentral() } }
          EOF

          cat <<'EOF' > app/build.gradle
          plugins { id 'com.android.application' }
          android {
              namespace 'com.turbo.net'
              compileSdk 34
              defaultConfig {
                  applicationId "com.turbo.net"
                  minSdk 21
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
          }
          dependencies {
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.9.0'
          }
          EOF
          echo "include ':app'" > settings.gradle

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.1.1

      - name: Build APK
        run: gradle assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NET-X-Application
          path: app/build/outputs/apk/debug/*.apk
